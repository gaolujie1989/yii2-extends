<?php

namespace lujie\amazon\sp\api;

use Yii;
use Iterator;

/**
* This class is autogenerated by the OpenAPI gii generator
* @description The Selling Partner API for Amazon Warehousing and Distribution (AWD) provides programmatic access to information about AWD shipments and inventory. 
*/
class Awd20240509 extends \lujie\amazon\sp\BaseAmazonSPClient
{

                
    /**
     * @description Retrieves an AWD inbound shipment.

**Usage Plan:**

| Rate (requests per second) | Burst |
| ---- | ---- |
| 2 | 2 |

The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, refer to [Usage Plans and Rate Limits in the Selling Partner API](https://developer-docs.amazon.com/sp-api/docs/usage-plans-and-rate-limits-in-the-sp-api)
     * @tag awd
     * @param string $shipmentId ID for the shipment. A shipment contains the cases being inbounded.
     * @param array $query
     *      - *skuQuantities* - string - optional
     *          - If equal to `SHOW`, the response includes the shipment SKU quantity details.

Defaults to `HIDE`, in which case the response does not contain SKU quantities
     * @return array
     *      - *carrierCode* - 
     *          - The shipment carrier code.
     *      - *createdAt* - string
     *          - Timestamp when the shipment was created. The date is returned in <a href='https://developer-docs.amazon.com/sp-api/docs/iso-8601'>ISO 8601</a> format.
     *      - *destinationAddress* - 
     *          - Destination address for this shipment.
     *      - *externalReferenceId* - string
     *          - Client-provided reference ID that can correlate this shipment to client resources. For example, to map this shipment to an internal bookkeeping order record.
     *      - *orderId* - string
     *          - The AWD inbound order ID that this inbound shipment belongs to.
     *      - *originAddress* - 
     *          - Origin address for this shipment.
     *      - *receivedQuantity* - array
     *          - Quantity received (at the receiving end) as part of this shipment.
     *      - *shipBy* - string
     *          - Timestamp when the shipment will be shipped.
     *      - *shipmentContainerQuantities* - array
     *          - Packages that are part of this shipment.
     *      - *shipmentId* - string
     *          - Unique shipment ID.
     *      - *shipmentSkuQuantities* - array
     *          - Quantity details at SKU level for the shipment. This attribute will only appear if the skuQuantities parameter in the request is set to SHOW.
     *      - *shipmentStatus* - 
     *          - Current status of this shipment.
     *      - *trackingId* - string
     *          - Carrier-unique tracking ID for this shipment.
     *      - *updatedAt* - string
     *          - Timestamp when the shipment was updated. The date is returned in <a href='https://developer-docs.amazon.com/sp-api/docs/iso-8601'>ISO 8601</a> format.
     *      - *warehouseReferenceId* - string
     *          - An AWD-provided reference ID that you can use to interact with the warehouse. For example, a carrier appointment booking.
     */
    public function getInboundShipment(string $shipmentId, array $query = []): array
    {
        return $this->api(array_merge(["/awd/2024-05-09/inboundShipments/{$shipmentId}"], $query));
    }
                
    /**
     * @description Retrieves a summary of all the inbound AWD shipments associated with a merchant, with the ability to apply optional filters.

**Usage Plan:**

| Rate (requests per second) | Burst |
| ---- | ---- |
| 1 | 1 |

The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, refer to [Usage Plans and Rate Limits in the Selling Partner API](https://developer-docs.amazon.com/sp-api/docs/usage-plans-and-rate-limits-in-the-sp-api).
     * @tag awd
     * @param array $query
     *      - *sortBy* - string - optional
     *          - Field to sort results by. By default, the response will be sorted by UPDATED_AT.
     *      - *sortOrder* - string - optional
     *          - Sort the response in ASCENDING or DESCENDING order. By default, the response will be sorted in DESCENDING order.
     *      - *shipmentStatus* - string - optional
     *          - Filter by inbound shipment status.
     *      - *updatedAfter* - string - optional
     *          - List the inbound shipments that were updated after a certain time (inclusive). The date must be in <a href='https://developer-docs.amazon.com/sp-api/docs/iso-8601'>ISO 8601</a> format.
     *      - *updatedBefore* - string - optional
     *          - List the inbound shipments that were updated before a certain time (inclusive). The date must be in <a href='https://developer-docs.amazon.com/sp-api/docs/iso-8601'>ISO 8601</a> format.
     *      - *maxResults* - integer - optional
     *          - Maximum number of results to return.
     *      - *nextToken* - string - optional
     *          - Token to retrieve the next set of paginated results.
     * @return Iterator
     *      - *nextToken* - string
     *          - Token to retrieve the next set of paginated results.
     *      - *shipments* - array
     *          - List of inbound shipment summaries.
     */
    public function eachtInboundShipments(array $query = []): Iterator
    {
        return $this->eachInternal('listInboundShipments', func_get_args());
    }
        
    /**
     * @description Retrieves a summary of all the inbound AWD shipments associated with a merchant, with the ability to apply optional filters.

**Usage Plan:**

| Rate (requests per second) | Burst |
| ---- | ---- |
| 1 | 1 |

The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, refer to [Usage Plans and Rate Limits in the Selling Partner API](https://developer-docs.amazon.com/sp-api/docs/usage-plans-and-rate-limits-in-the-sp-api).
     * @tag awd
     * @param array $query
     *      - *sortBy* - string - optional
     *          - Field to sort results by. By default, the response will be sorted by UPDATED_AT.
     *      - *sortOrder* - string - optional
     *          - Sort the response in ASCENDING or DESCENDING order. By default, the response will be sorted in DESCENDING order.
     *      - *shipmentStatus* - string - optional
     *          - Filter by inbound shipment status.
     *      - *updatedAfter* - string - optional
     *          - List the inbound shipments that were updated after a certain time (inclusive). The date must be in <a href='https://developer-docs.amazon.com/sp-api/docs/iso-8601'>ISO 8601</a> format.
     *      - *updatedBefore* - string - optional
     *          - List the inbound shipments that were updated before a certain time (inclusive). The date must be in <a href='https://developer-docs.amazon.com/sp-api/docs/iso-8601'>ISO 8601</a> format.
     *      - *maxResults* - integer - optional
     *          - Maximum number of results to return.
     *      - *nextToken* - string - optional
     *          - Token to retrieve the next set of paginated results.
     * @return Iterator
     *      - *nextToken* - string
     *          - Token to retrieve the next set of paginated results.
     *      - *shipments* - array
     *          - List of inbound shipment summaries.
     */
    public function batchtInboundShipments(array $query = []): Iterator
    {
        return $this->batchInternal('listInboundShipments', func_get_args());
    }
    
    /**
     * @description Retrieves a summary of all the inbound AWD shipments associated with a merchant, with the ability to apply optional filters.

**Usage Plan:**

| Rate (requests per second) | Burst |
| ---- | ---- |
| 1 | 1 |

The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, refer to [Usage Plans and Rate Limits in the Selling Partner API](https://developer-docs.amazon.com/sp-api/docs/usage-plans-and-rate-limits-in-the-sp-api).
     * @tag awd
     * @param array $query
     *      - *sortBy* - string - optional
     *          - Field to sort results by. By default, the response will be sorted by UPDATED_AT.
     *      - *sortOrder* - string - optional
     *          - Sort the response in ASCENDING or DESCENDING order. By default, the response will be sorted in DESCENDING order.
     *      - *shipmentStatus* - string - optional
     *          - Filter by inbound shipment status.
     *      - *updatedAfter* - string - optional
     *          - List the inbound shipments that were updated after a certain time (inclusive). The date must be in <a href='https://developer-docs.amazon.com/sp-api/docs/iso-8601'>ISO 8601</a> format.
     *      - *updatedBefore* - string - optional
     *          - List the inbound shipments that were updated before a certain time (inclusive). The date must be in <a href='https://developer-docs.amazon.com/sp-api/docs/iso-8601'>ISO 8601</a> format.
     *      - *maxResults* - integer - optional
     *          - Maximum number of results to return.
     *      - *nextToken* - string - optional
     *          - Token to retrieve the next set of paginated results.
     * @return array
     *      - *nextToken* - string
     *          - Token to retrieve the next set of paginated results.
     *      - *shipments* - array
     *          - List of inbound shipment summaries.
     */
    public function listInboundShipments(array $query = []): array
    {
        return $this->api(array_merge(["/awd/2024-05-09/inboundShipments"], $query));
    }
                
    /**
     * @description Lists AWD inventory associated with a merchant with the ability to apply optional filters.

**Usage Plan:**

| Rate (requests per second) | Burst |
| ---- | ---- |
| 2 | 2 |

The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, refer to [Usage Plans and Rate Limits in the Selling Partner API](https://developer-docs.amazon.com/sp-api/docs/usage-plans-and-rate-limits-in-the-sp-api).
     * @tag awd
     * @param array $query
     *      - *sku* - string - optional
     *          - Filter by seller or merchant SKU for the item.
     *      - *sortOrder* - string - optional
     *          - Sort the response in `ASCENDING` or `DESCENDING` order.
     *      - *details* - string - optional
     *          - Set to `SHOW` to return summaries with additional inventory details. Defaults to `HIDE,` which returns only inventory summary totals.
     *      - *nextToken* - string - optional
     *          - Token to retrieve the next set of paginated results.
     *      - *maxResults* - integer - optional
     *          - Maximum number of results to return.
     * @return Iterator
     *      - *inventory* - array
     *          - List of inventory summaries.
     *      - *nextToken* - string
     *          - Token to retrieve the next set of paginated results.
     */
    public function eachtInventory(array $query = []): Iterator
    {
        return $this->eachInternal('listInventory', func_get_args());
    }
        
    /**
     * @description Lists AWD inventory associated with a merchant with the ability to apply optional filters.

**Usage Plan:**

| Rate (requests per second) | Burst |
| ---- | ---- |
| 2 | 2 |

The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, refer to [Usage Plans and Rate Limits in the Selling Partner API](https://developer-docs.amazon.com/sp-api/docs/usage-plans-and-rate-limits-in-the-sp-api).
     * @tag awd
     * @param array $query
     *      - *sku* - string - optional
     *          - Filter by seller or merchant SKU for the item.
     *      - *sortOrder* - string - optional
     *          - Sort the response in `ASCENDING` or `DESCENDING` order.
     *      - *details* - string - optional
     *          - Set to `SHOW` to return summaries with additional inventory details. Defaults to `HIDE,` which returns only inventory summary totals.
     *      - *nextToken* - string - optional
     *          - Token to retrieve the next set of paginated results.
     *      - *maxResults* - integer - optional
     *          - Maximum number of results to return.
     * @return Iterator
     *      - *inventory* - array
     *          - List of inventory summaries.
     *      - *nextToken* - string
     *          - Token to retrieve the next set of paginated results.
     */
    public function batchtInventory(array $query = []): Iterator
    {
        return $this->batchInternal('listInventory', func_get_args());
    }
    
    /**
     * @description Lists AWD inventory associated with a merchant with the ability to apply optional filters.

**Usage Plan:**

| Rate (requests per second) | Burst |
| ---- | ---- |
| 2 | 2 |

The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, refer to [Usage Plans and Rate Limits in the Selling Partner API](https://developer-docs.amazon.com/sp-api/docs/usage-plans-and-rate-limits-in-the-sp-api).
     * @tag awd
     * @param array $query
     *      - *sku* - string - optional
     *          - Filter by seller or merchant SKU for the item.
     *      - *sortOrder* - string - optional
     *          - Sort the response in `ASCENDING` or `DESCENDING` order.
     *      - *details* - string - optional
     *          - Set to `SHOW` to return summaries with additional inventory details. Defaults to `HIDE,` which returns only inventory summary totals.
     *      - *nextToken* - string - optional
     *          - Token to retrieve the next set of paginated results.
     *      - *maxResults* - integer - optional
     *          - Maximum number of results to return.
     * @return array
     *      - *inventory* - array
     *          - List of inventory summaries.
     *      - *nextToken* - string
     *          - Token to retrieve the next set of paginated results.
     */
    public function listInventory(array $query = []): array
    {
        return $this->api(array_merge(["/awd/2024-05-09/inventory"], $query));
    }
    
}
