<?php

namespace lujie\amazon\advertising\api;

use Yii;
use Iterator;

/**
* This class is autogenerated by the OpenAPI gii generator
* @description Check advertising eligibility of global offers.
*/
class EligibilityV3 extends \lujie\amazon\advertising\BaseAmazonAdvertisingClient
{

                
    /**
     * @description Gets a list of advertising eligibility objects for a set of products. Requests are permitted only for products sold by the merchant associated with the profile. Note that the request object is a list of ASINs, but multiple SKUs are returned if there is more than one SKU associated with an ASIN. If a product is not eligible for advertising, the response includes an object describing the reasons for ineligibility.

**Requires one of these permissions**:
["advertiser_campaign_edit","advertiser_campaign_view"]     * @tag Product Eligibility
     * @param array $data Request for Product Eligibility
     *      - *adType* - string
     *          - Set to 'sp' to check product eligibility for Sponsored Products advertisements. Set to 'sb' to check product eligibility for Sponsored Brands advertisements. Set to 'sd' to check product eligibility for Sponsored Displays advertisements. Set to 'dsp' to check product eligibility for Demand Side Platform advertisements.
     *      - *productDetailsList* - array
     *          - A list of product identifier objects.
     *      - *locale* - string
     *          - Set to the locale string in the table below to specify the language in which the response is returned
     * @return array
     *      - *productResponseList* - array
     *          - A list of product advertising eligibility responses.
     */
    public function productEligibility(array $data, string $contentType = 'application/json'): array
    {
        return $this->api("/eligibility/product/list", 'POST', $data, ['content-type' => $contentType, 'accept' => $contentType]);
    }
                    
    /**
     * @description Checks the advertiser's eligibility to ad programs.     * @tag Program Eligibility
     * @param array $data 
     *      - *skipChecks* - 
     * @return array
     *      - *eligibilityStatusMap* - 
     */
    public function programEligibility(array $data, string $contentType = 'application/json'): array
    {
        return $this->api("/eligibility/programs", 'POST', $data, ['content-type' => $contentType, 'accept' => $contentType]);
    }
    
}
